import { motion } from 'framer-motion';
import SectionTitle from './SectionTitle';

const Contact = () => {
  return (
    <motion.section
    id='contact'
    className='max-x-contentContainer mx-auto py-20'
    >
      <SectionTitle title="Let's Connect!" titleNo='04' className='' />
      <div>

      </div>
    </motion.section>
  )
}

export default Contact
